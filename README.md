# portail-hotel — Nhalabene AGENCIAS

Dossier **autonome et complet** (tout est dedans).

## Contenu
- `index.html` — application
- `shared/` — `config.js`, `i18n.js`, `nhb-theme.css`, `nhb-ops.js`, `nhb-boot.js`
- copies plates à la racine (`config.js`, `i18n.js`, …) pour GitHub Pages
- `assets/` — logos Nhalabene
- `manifest.webmanifest` + `sw.js` — PWA
- `LICENSE`

## Config API
1. Ouvrir `index.html`
2. Coller l'URL `/exec` dans `AGENCIAS_API_URL` en haut du fichier
   **ou** dans `shared/config.js` / `config.js` → `API_URL`

Backend : `../backend/AGENCIAS_ERP.gs.txt`  
Docs : `../docs/`
